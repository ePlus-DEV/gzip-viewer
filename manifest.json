{
  "manifest_version": 3,
  "name": "GZIP JSONL Viewer",
  "version": "1.1.0",
  "description": "View and validate remote JSON, JSONL and GZIP JSONL feeds directly in Chrome.",
  "permissions": ["tabs", "storage"],
  "host_permissions": ["<all_urls>"],
  "action": {
    "default_popup": "popup.html",
    "default_title": "GZIP JSONL Viewer"
  }
}
